en:
  application:
    name: VPM Registry
    owner: eni
    description: "Unofficial VPM registry"
    keywords: vpm registry
    support_email: change.me@localhost
    key: vpm_registry
  webhooks/outgoing/endpoints:
    fields:
      api_version:
        options:
          1: V1
          # 🚅 super scaffolding will insert new api versions above this line.
